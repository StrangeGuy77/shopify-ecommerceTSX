/**
 * @description Specifies the input fields required for an attribute.
 * @param key (string) Key or name of the attribute.
 * @param value (string) Value of the attribute.
 */

export interface AttributeInput {
	key: string;
	value: string;
}
